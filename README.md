# AI Ecommerce Bot

AI 电商助手 MVP：商品管理、AI 文案生成、AI 客服、Docker、Render 部署。

## 快速部署
1. Render 选择 Blueprint 并连接此仓库。
2. 保持 `AI_DEMO_MODE=true` 可先免费体验。
3. 有 AI API 后，在 Render 环境变量设置 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。

本项目默认后端同时提供 PC 管理页面和 API。
